# PORT is read directly by the start command (uvicorn --port / the Docker
# CMD's ${PORT:-8000}), not by application code — hosting platforms like
# Render inject it automatically, so it rarely needs to be set by hand.
PORT=8000
HEADLESS=true
NAV_TIMEOUT_MS=20000
